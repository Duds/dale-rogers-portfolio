export interface MyValuesProps {
  values: Array<{
    title: string;
    description: string;
    icon: string;
  }>;
}

export interface WhoIsDaleProps {
  image: string;
  bio: string;
  skills: string[];
}
