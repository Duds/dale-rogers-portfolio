---
/**
 * Generic page header component for consistent page headers across the site
 * @component PageHeader
 *
 * @example
 * ```astro
 * <PageHeader
 *   title="Case Studies"
 *   description="Explore my portfolio of successful projects."
 * />
 * ```
 */
interface Props {
  /** The main title for the page header */
  title: string;
  /** Descriptive text for the page header */
  description: string;
}

const { title, description } = Astro.props;
---

<section
  class="pt-32 md:pt-40 py-16 px-6 md:px-12 bg-neutral-50 dark:bg-neutral-900"
>
  <div class="max-w-6xl mx-auto">
    <h1
      class="text-4xl md:text-5xl lg:text-6xl font-display font-bold text-primary dark:text-primary mb-6"
    >
      {title}
    </h1>
    <p
      class="text-xl md:text-2xl text-neutral-600 dark:text-neutral-300 max-w-3xl"
    >
      {description}
    </p>
  </div>
</section>
