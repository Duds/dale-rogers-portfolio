# Enforce pnpm as the package manager
package-manager=pnpm

# Prevent accidental use of npm/yarn
engine-strict=true

# Use pnpm store for better performance
store-dir=.pnpm-store

# Ensure consistent package resolution
shamefully-hoist=false
